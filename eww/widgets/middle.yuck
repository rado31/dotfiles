(defpoll time :interval "1s"
    `date "+%d-%m-%Y ─ %H:%M:%S"`
)

(defwidget middle []
    (box :halign "center"
        (eventbox :onclick "~/.config/eww/scripts/calendar.bash" :cursor "pointer"
            (label :text time :class "time")
        )
    )
)
