(defwindow bar
    :monitor 0
    :geometry (geometry
        :y "10px"
        :width "98%"
        :anchor "top center"
    )
    :exclusive true

    (box :class "layout"
        (left)
        (middle)
        (right)
    )
)
